{
  "private": true,
  "scripts": {
    "check": "oxlint && oxfmt --check",
    "fix": "oxlint --fix && oxfmt --write"
  },
  "devDependencies": {
    "oxfmt": "^0.62.0",
    "oxlint": "^1.77.0"
  }
}
