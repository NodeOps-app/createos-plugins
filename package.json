{
  "name": "@createos/pi",
  "version": "0.2.0",
  "private": true,
  "description": "Pi coding agent extension with CreateOS Sandbox tools and optional remote tool routing",
  "keywords": [
    "createos",
    "pi-extension",
    "pi-package",
    "sandbox"
  ],
  "type": "module",
  "scripts": {
    "check": "oxlint && oxfmt --check",
    "fix": "oxlint --fix && oxfmt --write"
  },
  "devDependencies": {
    "oxfmt": "^0.63.0",
    "oxlint": "^1.77.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "^0.84.1",
    "@earendil-works/pi-coding-agent": "^0.84.1",
    "typebox": "^1.1.38"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  }
}
