{
  "schemaVersion": 1,
  "id": "createos-sandbox",
  "name": "CreateOS Sandbox",
  "description": "Firecracker microVM reached over the CreateOS SSH gateway",
  "checkoutMode": "provisioned-root",
  "create": "createos setup orca --recipe",
  "destroy": "createos setup orca --recipe"
}
